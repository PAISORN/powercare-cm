# PowerCare Asset Refactor — Codex Pack

เริ่มจากไฟล์ `CODEX_TASK.md`

แนะนำให้ Copy โฟลเดอร์นี้เข้า repo เช่น `docs/asset-refactor/` แล้วเปิด Codex ที่ root ของ repo

คำสั่งเริ่มงาน:
`Read docs/asset-refactor/CODEX_TASK.md and implement the Asset refactor. Inspect the existing code before editing.`

## Asset Code
อ่าน `ASSET_CODE_STANDARD.md` สำหรับกติกา Asset Code ใหม่
และ `asset_code_master.csv` สำหรับตัวอย่าง code/type mapping ที่ใช้อยู่

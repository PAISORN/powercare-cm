export type PmSummary = { today: number; planned: number; inProgress: number; overdue: number; completed: number; abnormal: number };
const labels: [keyof PmSummary, string][] = [["today", "Today"], ["planned", "Planned"], ["inProgress", "In Progress"], ["overdue", "Overdue"], ["completed", "Completed"], ["abnormal", "Abnormal"]];
export function PmSummaryStrip({ summary }: { summary: PmSummary }) { return <section aria-label="PM summary" className="grid min-w-0 grid-cols-2 gap-3 sm:grid-cols-3 xl:grid-cols-6">{labels.map(([key, label]) => <article className="min-w-0 rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-4 shadow-sm" key={key}><p className="truncate text-xs font-bold text-[var(--muted)]">{label}</p><p className="mt-1 text-2xl font-black">{summary[key]}</p></article>)}</section>; }
